package com.example.menuapp.ui.theme

import androidx.compose.ui.graphics.Color

// Pizza Paradize Color Palette
val Primary = Color(0xFFA02C2C) // Deep, warm maroon red
val Secondary = Color(0xFFF0A828) // Bright, golden yellow
val BackgroundLight = Color(0xFFFDFBEA) // Light cream or off-white

val TextOnPrimary = Color(0xFFF0A828) // Golden yellow text on maroon background
val TextOnSecondary = Color(0xFFA02C2C) // Maroon red text on golden yellow background
val TextStandard = Color(0xFF333333) // Dark grey for standard text

val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

val IconColor = Secondary // Use the accent color for icons
val CardBackgroundColor = BackgroundLight // Use the light cream for card backgrounds
val ChevronColor = Color(0x80000000) // black with 50% opacity
val DarkChevronColor = Color(0x80FFFFFF) // white with 50% opacity (will be removed)
