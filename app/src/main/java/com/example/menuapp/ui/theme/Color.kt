package com.example.menuapp.ui.theme

import androidx.compose.ui.graphics.Color

// Colors based on the app logo
val LogoOrange = Color(0xFFF4511E)
val LogoDarkGray = Color(0xFF121212)

val Primary = LogoOrange
val BackgroundLight = Color(0xFFF8F6F5)
val BackgroundDark = LogoDarkGray

// Define other colors from the design if needed
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val TextBlack = Color(0xFF000000)
val TextWhite = Color(0xFFFFFFFF)
val IconColor = Primary
val CardBackgroundColor = White
val DarkCardBackgroundColor = Color(0xFF1E1E1E) // A slightly lighter shade for dark cards
val ChevronColor = Color(0x80000000) // black with 50% opacity
val DarkChevronColor = Color(0x80FFFFFF) // white with 50% opacity
